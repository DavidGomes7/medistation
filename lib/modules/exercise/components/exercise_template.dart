import 'package:flutter/material.dart';

class ExerciseTemplate extends StatelessWidget {
  const ExerciseTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('exerise'),
      ),
    );
  }
}
