import 'package:flutter/material.dart';

class GoalTemplate extends StatelessWidget {
  const GoalTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('goal'),
      ),
    );
  }
}
