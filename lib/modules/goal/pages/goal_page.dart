import 'package:flutter/material.dart';

import '../components/components.dart';

class GoalPage extends StatelessWidget {
  const GoalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const GoalTemplate();
  }
}
