import 'package:flutter/material.dart';

import '../components/components.dart';

class TipsPage extends StatelessWidget {
  const TipsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const TipsTemplate();
  }
}
