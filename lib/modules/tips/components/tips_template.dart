import 'package:flutter/material.dart';

class TipsTemplate extends StatelessWidget {
  const TipsTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('tips'),
      ),
    );
  }
}
